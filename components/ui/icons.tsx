import { User, UserPlus, Loader2, CheckCircle, Printer } from 'lucide-react'

export const Icons = {
  user: User,
  userPlus: UserPlus,
  spinner: Loader2,
  checkCircle: CheckCircle,
  printer: Printer,
}

